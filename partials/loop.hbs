<section class="section-loop relative">
    <div class="grid grid-cols-1 xl:grid-cols-3 js-post-list">
        {{#foreach posts}}
            {{> post-card}}
        {{/foreach}}
    </div>
</section>